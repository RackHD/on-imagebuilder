---
- include: common/playbooks/initrd_wrapper.yml
  vars:
    - config_file: vars/initrd.yml

- include: common/playbooks/basefs_wrapper.yml
  vars:
    - config_file: vars/basefs.yml

- include: common/playbooks/overlay_wrapper.yml
  vars:
    - config_file: vars/discovery_overlay.yml
